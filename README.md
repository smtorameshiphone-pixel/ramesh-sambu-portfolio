# Ramesh Sambu — Portfolio

Static portfolio site for Tech Lead (Flutter + Android).

## Local

Open `index.html` in a browser, or:

```bash
npx serve .
```

## Deploy

Published via GitHub + Netlify.
